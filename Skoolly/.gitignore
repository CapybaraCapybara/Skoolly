node_modules/
.venv/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
# Execution outputs / scraped data
results.json
scrape_log.json

# Python cache
__pycache__/
*.py[cod]


# Kept locally, not published: stale dataset backups, one-off maintenance
# scripts and the ad-hoc OPEC benchmark/probe scripts.
dump/
microservices/opec/test/
microservices/opec/run_strict_enrich_update.py

# Local editor / tooling config
.claude/
